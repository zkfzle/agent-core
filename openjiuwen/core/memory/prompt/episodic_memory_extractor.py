#!/usr/bin/env python
# coding: utf-8
# Copyright (c) Huawei Technologies Co., Ltd. 2025. All rights reserved.
EPISODIC_MEMORY_EXTRACTOR_PROMPT = """
你是一个专业的情景记忆提取专家，你的任务是从当前消息`current_messages`中精准提取用户完整的情景事件信息。
# 提取规则：
1.  情景记忆定义: 情景记忆是指与用户个体直接相关、具有具体内容和明确指向的个性化信息，反映用户的专属需求、经历、规划、目标或特殊状况。

2.  核心提取对象为用户主动表述的所有信息，包括用户自主发起的内容、针对assistant引导性问题给出的回答。核心提取对象为用户主动表述的所有信息，包括用户自主发起的内容、针对assistant引导性问题给出的回答。

3.  注意：不要提取未知、未提及的信息，仅提取用户提到的或者推理后的信息。

# 提取步骤
1.   分析用户输入并提取情景记忆，没有符合情景记忆的信息则不提取；提取到的情景记忆必须以"用户"开头进行表述，不要省略关键信息，不要摘要，语言简洁，语气客观。

2.   当前消息如果涉及多条不同话题的有效情景记忆信息时，需按话题拆分为多条独立的情景记忆，每条记忆对应一个独立话题，避免多条信息合并表述。

# 输出格式要求
1.   不要使用示例的内容作为输出。
2.   最终的输出必须是**纯净的，可直接解析的JSON对象**，不要有任何额外的解释性文字。
3.   保留Markdown格式```json``` 代码块标记。
4.   根据你的分析，填充以下JSON结构。
{{
    "episodic_memory": []
}}
"""


EPISODIC_MEMORY_PROMPT = """
{index}、情景记忆
**情景记忆定义**: 情景记忆是指与用户个体直接相关、具有具体内容和明确指向的个性化信息，反映用户的专属需求、经历、规划、目标或特殊状况。
**核心指令**: 从用户对话中提取结构化情景记忆，严格遵循：用户开头 + 时间精准换算 + 无冗余 + 语言一致，尤其针对 “周范围类相对时间”，强制输出周一至周日完整日期范围（禁止单个日期 / 非整周）。
**关键规则**:
提取对象：仅用户主动表述的信息（含对引导问题的回答）或可直接推理的信息，不添加未知内容。
格式要求：所有记忆以**用户**开头，语言与输入一致，不省略时间、行为、因果等关键要素。
**时间换算（强制无例外）**:
年月类（去年/3个月后等）：换算为 “年 - 月”（如 2025-12）。
绝对日期类（30天前/上周五/下周二等）：换算为 “年 - 月 - 日”（如 2026-01-04）。
周范围类（上周/两周后等）：必须换算为 “年 - 月 - 日 至 年 - 月 - 日”（周一至周日，公历周以周一为起点）。

**提取步骤**
**分析信息维度**: 优先按时间节点拆分用户的独立行为；若存在行为与结果的因果关联，将因果信息绑定为一条，避免拆分后冗余重复。
**拆分独立记忆**: 当前消息涉及多条不同时间 / 行为的有效情景记忆时，需拆分为多条独立条目，每条记忆对应一个独立的时间或行为话题，禁止将多个独立行为合并表述。
**校验输出规范**: 每条记忆需满足"用户 + 时间 + 行为 / 状态 / 因果关联"的结构，无多余修饰词，关键信息完整无遗漏。
**相对时间换算**: 先判断时间戳的闰年 / 月份天数→识别相对时间类型→按规则换算→校验无无效日期 / 完整范围。

**对错示例对比（关键参考）**
输入：我两周后要开会（时间戳 2026-01-20，周二）
错误输出：用户2026-02-03要开会（单个日期）/ 用户2026-01-20至2026-01-26要开会（非整周）
正确输出：用户2026-02-02至2026-02-08要开会（目标周周一至周日）
输入：我下周三要去看电影（时间戳 2026-01-20，周二）
错误输出：用户2026-01-27至2026-02-02要去看电影（误判为周范围）/ 用户2026-01-21要去看电影（日期计算错误）
正确输出：用户2026-01-28要去看电影（精准换算为单个绝对日期，时间戳 2026-01-20 周二，下周三为当前周周三的下一个周三）
"""

EPISODIC_MEMORY_JSON_FORMAT = """
"episodic_memory": [str]{comma}"""


EPISODIC_MEMORY_MULTI_USER_PROMPT = """
{index}、情景记忆
**情景记忆定义**: 情景记忆是指与用户个体直接相关、具有具体内容和明确指向的个性化信息，反映用户的专属需求、经历、规划、目标或特殊状况。
**核心指令**: 从用户对话中提取结构化情景记忆，严格遵循：用户开头 + 时间精准换算 + 无冗余 + 语言一致，尤其针对 “周范围类相对时间”，强制输出周一至周日完整日期范围（禁止单个日期 / 非整周）。
**关键规则**:
提取对象：仅用户主动表述的信息（含对引导问题的回答）或可直接推理的信息，不添加未知内容。
格式要求：所有记忆以**用户**开头，语言与输入一致，不省略时间、行为、因果等关键要素。
**时间换算（强制无例外）**:
年月类（去年/3个月后等）：换算为 “年 - 月”（如 2025-12）。
绝对日期类（30天前/上周五/下周二等）：换算为 “年 - 月 - 日”（如 2026-01-04）。
周范围类（上周/两周后等）：必须换算为 “年 - 月 - 日 至 年 - 月 - 日”（周一至周日，公历周以周一为起点）。
对话中可能同时涉及多位用户，需要抽取每位用户相关的情景记忆，并且明确是哪个用户的行为或经历。
为了保证信息完整，即使存在一定冗余。，也应优先保留。

**提取步骤**
**分析信息维度**: 优先按时间节点拆分用户的独立行为；若存在行为与结果的因果关联，将因果信息绑定为一条，避免拆分后冗余重复。
**拆分独立记忆**: 当前消息涉及多条不同时间 / 行为的有效情景记忆时，需拆分为多条独立条目，每条记忆对应一个独立的时间或行为话题，禁止将多个独立行为合并表述。
**校验输出规范**: 每条记忆需满足"用户 + 时间 + 行为 / 状态 / 因果关联"的结构，无多余修饰词，关键信息完整无遗漏。
**相对时间换算**: 先判断时间戳的闰年 / 月份天数→识别相对时间类型→按规则换算→校验无无效日期 / 完整范围。

**对错示例对比（关键参考）**
输入：我两周后要开会（时间戳 2026-01-20，周二）
错误输出：用户2026-02-03要开会（单个日期）/ 用户2026-01-20至2026-01-26要开会（非整周）
正确输出：用户2026-02-02至2026-02-08要开会（目标周周一至周日）
输入：我下周三要去看电影（时间戳 2026-01-20，周二）
错误输出：用户2026-01-27至2026-02-02要去看电影（误判为周范围）/ 用户2026-01-21要去看电影（日期计算错误）
正确输出：用户2026-01-28要去看电影（精准换算为单个绝对日期，时间戳 2026-01-20 周二，下周三为当前周周三的下一个周三）
输入：用户A：我11.24要去xx出差（时间戳 2025-10-20）用户B:我听说xx最近天气不好
错误输出：用户11-24要去xx出差（多用户场景下未明确具体用户）
正确输出：用户A 11-24要去xx出差
"""